// let attendlist = realm.objects("AttendModel");
var INITAL_STATE = {
	// data: attendlist,
	refreshing: false,
	items: [],
};


export default (state = INITAL_STATE, action) => {
	switch (action.type) {



		default:
			return state;
	}
};

